# Logo

Place your logo here. Recommended: SVG or PNG with transparent background, minimum 400px wide.
File name: `logo.png` or `logo.svg`

Navbar currently uses a styled text logo as placeholder. When you drop a real file here, update `<img src>` attributes in HTML to match the filename.
